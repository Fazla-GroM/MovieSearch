import React from 'react';
import { css } from '@emotion/core';

const AboutPage = props => {
    const cssPage = css({});

    return <main css={cssPage}>AboutPage</main>;
};

export default AboutPage;
